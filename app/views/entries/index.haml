:javascript


%h1="To Do List"
- @dates.each do |date|
  - datestr = date.strftime("%m_%d_%Y")
  .date_container
    %h3= date_label(date)
  .add_entry{:id => "add_entry_#{datestr}"}= "add"
  .add_entry_controls{:id => "add_entry_#{datestr}"}
    %div= "Time"
    %div= text_field_tag("time_field_#{datestr}")
    %div= "Content"
    %div= text_field_tag("content_field_#{datestr}")
    %div= submit_tag("Add")
  - Entry.where("due_date = ?",date).order("order_time").each do |entry|
    .entry
      %span.entry_time= entry.due_time ? entry.due_time : ""
      %span.entry_content= entry.content
      %span.entry_controls= ""



