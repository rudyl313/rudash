%table
  - daily.each do |item|
    %tr
      %td
        != item.due_time ? item.due_time.strftime("%I:%M%p") : "&nbsp;"
      %td
        = item.content
      %td.destroy_rentry{:"data-id" => item.id}
        = "Destroy"