- monthly.each do |item|
  .recurring_entry_time
    != item.due_time ? item.due_time.strftime("%I:%M%p") : "&nbsp;"
  .recurring_entry_mday
    = item.mday
  .recurring_entry_content
    = item.content